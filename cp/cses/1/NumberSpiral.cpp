#include <bits/stdc++.h>
#define ll long long
using namespace std;

int main()
{
    ll t;
    ll x, ll y;
    while (--t > 0)
    {
        cin >> y >> x;

        ll n;

        if (y >= x)
        {
            if (y % 2 == 0) {}
            else {

            }
        }
        else
        {
            
        }
        cout << n << endl;
    }

    return 0;
};